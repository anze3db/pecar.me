{
  "dependencies": {
    "autoprefixer": "^10.2.3",
    "postcss": "^8.2.4",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.0.2"
  },
  "scripts": {
    "publish": "NODE_ENV=production node_modules/.bin/postcss style.css -o compiled.css --verbose",
    "dev": "node_modules/.bin/postcss style.css -o compiled.css --verbose"
  }
}